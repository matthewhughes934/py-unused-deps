[project]
name = "dist-dep-in-tests"
version = "0.0.1"
dependencies = [
    "py-unused-deps-testing-foo",
    "py-unused-deps-testing-bar",
]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
exclude = ["tests*"]
